# DashyBoard
B2C DashyBoard: personlig dashboard med inloggning och sparad widget-konfiguration

# Run in Docker

This will start everything:

    docker compose up -d

If you only wish to start the frontend/backend only

    docker compose up -d dashyboard-backend

If you are using docker desktop.
You can figure it out on your own :)
